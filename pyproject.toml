[project]
name = "poly-research"
version = "0.1.0"
description = "Python research utilities for the Poly trading bot"
requires-python = ">=3.11"
dependencies = ["numpy>=2.2,<3"]

[project.optional-dependencies]
train = ["torch>=2.5,<3"]

[tool.uv]
package = false
